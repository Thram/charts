apiVersion: v1
kind: Service
metadata:
  name: {{ include "drone-server-chart.fullname" . }}
  labels:
    {{- include "drone-server-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "drone-server-chart.selectorLabels" . | nindent 4 }}
