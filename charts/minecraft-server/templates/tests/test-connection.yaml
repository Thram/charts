apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "minecraft-chart.fullname" . }}-test-connection"
  labels:
    {{- include "minecraft-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "minecraft-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
