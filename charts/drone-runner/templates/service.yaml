apiVersion: v1
kind: Service
metadata:
  name: {{ include "drone-runner-chart.fullname" . }}
  labels:
    {{- include "drone-runner-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "drone-runner-chart.selectorLabels" . | nindent 4 }}
