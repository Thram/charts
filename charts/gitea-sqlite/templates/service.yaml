apiVersion: v1
kind: Service
metadata:
  name: {{ include "gitea-basic.fullname" . }}
  labels:
    {{- include "gitea-basic.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gitea-basic.selectorLabels" . | nindent 4 }}
