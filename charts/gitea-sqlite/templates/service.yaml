apiVersion: v1
kind: Service
metadata:
  name: {{ include "gitea-basic-chart.fullname" . }}
  labels:
    {{- include "gitea-basic-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gitea-basic-chart.selectorLabels" . | nindent 4 }}
