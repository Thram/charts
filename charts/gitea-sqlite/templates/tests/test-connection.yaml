apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gitea-basic.fullname" . }}-test-connection"
  labels:
    {{- include "gitea-basic.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gitea-basic.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
